services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    volumes:
      - db-data:/data
      - opencode-sessions:/root/.local/share/opencode
      - /Users/loicmarcos/Desktop:/workspaces/user-data/desktop
    environment:
      - DATABASE_URL=sqlite:////data/routines.db
      - WORKSPACES_DIR=/workspaces/user-data
      - LOG_LEVEL=${LOG_LEVEL:-info}
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        - VITE_DEV=${VITE_DEV:-}
    ports:
      - '8080:80'
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  db-data:
  opencode-sessions:
  workspaces:
